# docker-curl
```
docker run index.alauda.cn/chennanfei/docker-curl \
  curl -X PUT -u <username>:<password> https://api.alauda.cn/v1/services/(namespace)/(service_name)/start

docker run index.alauda.cn/chennanfei/docker-curl \
  curl -X PUT -u <username>:<password> https://api.alauda.cn/v1/services/(namespace)/(service_name)/stop
```
